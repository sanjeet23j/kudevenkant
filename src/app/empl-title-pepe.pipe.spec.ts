import { EmplTitlePepePipe } from './empl-title-pepe.pipe';

describe('EmplTitlePepePipe', () => {
  it('create an instance', () => {
    const pipe = new EmplTitlePepePipe();
    expect(pipe).toBeTruthy();
  });
});
